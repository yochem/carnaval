name: Commit generated file to another repo

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  commit-and-pr:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout this repository
        uses: actions/checkout@v2

      - name: Checkout target repository
        run: |
          git clone git@github.com:yochem/yochem.github.io.git website
        env:
          GITHUB_TOKEN: ${{ secrets.PAT }}

      - name: Generate file
        run: echo "test" > website/test

      - name: Create new branch
        run: |
          cd website
          git checkout -b new-calendar

      - name: Commit changes
        run: |
          cd website
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          git add test
          git commit -m 'test: add test filel'
          git push origin new-calendar
        env:
          GITHUB_TOKEN: ${{ secrets.PAT }}

      - name: Create Pull Request
        uses: peter-evans/create-pull-request@v5
        with:
          token: ${{ secrets.PAT }}
          repository: yochem/yochem.github.io
          head: new-calendar
          title: 'Add generated file'
          body: 'This PR adds the generated file.'

